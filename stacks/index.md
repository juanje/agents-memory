# Stacks

Per-language/platform standards. Create a subdirectory for each stack
as you work on projects using it.

(No stacks registered yet. The sessionStart hook auto-detects stacks
from project files and loads standards if available.)
